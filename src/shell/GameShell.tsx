// Placeholder shell: replaced by the full menu flow later in the build.
export function GameShell() {
  return (
    <div className="flex h-screen items-center justify-center">
      <h1
        className="font-display text-6xl uppercase"
        style={{ color: "#E8B928", textShadow: "3px 3px 0 #000, 6px 6px 0 #B93A20" }}
      >
        Banjopocalypse
      </h1>
    </div>
  );
}
